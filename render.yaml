services:
  - type: web
    name: scraper-backend
    runtime: node
    plan: free
    branch: main
    rootDir: backend
    buildpacks:
      - url: https://github.com/render-examples/puppeteer-chromium
    buildCommand: "npm install"
    startCommand: "node src/server.js"
    envVars:
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/google-chrome-stable
      - key: NODE_VERSION
        value: "20"      
        